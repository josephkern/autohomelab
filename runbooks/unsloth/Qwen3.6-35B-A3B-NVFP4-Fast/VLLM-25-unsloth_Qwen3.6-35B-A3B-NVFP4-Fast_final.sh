#!/usr/bin/env bash
# FINAL (campaign-selected) — promoted from runbooks/unsloth/Qwen3.6-35B-A3B-NVFP4-Fast/baseline.sh on 2026-07-12.
# Result: Champion-parity A/B vs RedHatAI quant (VLLM-24 _final): chat c16 404.95 (+12.7%) / c1 76.98 (+39.5%) N=3 medians; quality tied-or-better (mmlu 81.32~81.72, gsm8k think-off 96.0, mmlu_pro 68.57>68.21); smoke 4/4 (known upstream xgrammar/MTP/reasoning artifact intermittent on ALL 0.25 thinking+MTP configs incl. old champion). unsloth NVFP4-Fast dethrones RedHatAI as the 35B canonical.
# Canonical config to serve. The *_tuned.sh experiment artifacts are kept intact as record.
# BASELINE (champion-parity) — unsloth/Qwen3.6-35B-A3B-NVFP4-Fast on gb10-1988a9714b4e.
# Generated by scripts/gen_baseline.py, then edited to CHAMPION-PARITY: this campaign's question is
# a checkpoint A/B — unsloth's "1.79x faster than other NVFP4 quants" claim (B200, 128-conc) vs our
# reigning RedHatAI quant — so every serving flag matches the VLLM-24 _final config (kernel path,
# MTP n=2, sampling, parsers), EXCEPT the image: v0.25.0 (current default; perf-neutral vs 0.24 on
# the champion, same-day matched pair 20260712). Same base model (Qwen3.6-35B-A3B), same arch
# (qwen3_5_moe), MTP head present + unquantized in this quant (ignore has re:^mtp.*).
# Champion references (same node, 20260712): chat c16=359.4 c1=55.18 (0.24) / 358.86, 56.01 (0.25);
# mmlu(LL,100) 81.72/81.79; drafter acceptance 68%.
# Confirm functional behavior via smoke; card recommends temp 0.6 for the BASE Qwen3.6 — we keep the
# champion's chat sampling (temp1/.95/k20/pp1.5) for parity; sampling is not part of the A/B.

MODEL="unsloth/Qwen3.6-35B-A3B-NVFP4-Fast"
MODEL_REVISION="1c3f884bc99aac2524f6d49bcbac8c88401afd66"   # pinned for reproducibility
SERVED_NAME="qwen3.6-35b-a3b-nvfp4-fast"

# Pinned backend image — v0.25.0 (backends/vllm/image.lock catalog, current default).
VLLM_IMAGE="vllm/vllm-openai@sha256:fc56161ee42a011aeee78b65d0a81b6683c7d04402fd40503d14d4d6c98f07cb"
VLLM_ENTRYPOINT_SERVE=true   # image ENTRYPOINT already runs `vllm serve`

VLLM_FLAGS=(
  # --- performance / resource (champion-parity) ---
  --tensor-parallel-size 1
  --gpu-memory-utilization 0.5
  --max-model-len 8192
  --max-num-batched-tokens 16384
  --enable-chunked-prefill
  # --- model-specific kernel path (champion-parity) ---
  --quantization compressed-tensors
  --kv-cache-dtype fp8_e4m3
  --moe-backend flashinfer_cutlass
  --speculative-config '{"method":"mtp","num_speculative_tokens":2}'
  --enable-prefix-caching
  # --- functional (champion-parity; smoke.sh validates) ---
  --enable-auto-tool-choice
  --tool-call-parser qwen3_coder
  --reasoning-parser qwen3
  --override-generation-config '{"temperature":1.0,"top_p":0.95,"top_k":20,"presence_penalty":1.5}'
)
# Optional env passed into the container.
VLLM_ENV=()
